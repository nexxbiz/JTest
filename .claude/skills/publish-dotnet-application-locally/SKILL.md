---
name: publish-dotnet-application-locally
description: Publish one explicitly selected generated .NET host to an explicit local output by using the backed Program Kit publish-local operation.
---

# publish-dotnet-application-locally

Load and follow the complete canonical definition at
`C:\Users\JoeyBarten\source\ai-era\program-kit\.agent-capabilities\capabilities\publish-dotnet-application-locally\CAPABILITY.md`
before acting.

This skill is only a manually authored Claude Code pointer. Its front matter
registers the trigger; the canonical provider-neutral capability owns the
procedure and boundaries.

It is not a registered Program Kit provider adapter: Program Kit has no Claude
Code provider in its capability source manifest or CLI allow-list, and this
file must not be described as Program Kit initialization or support.

This capability is a repository-only wrapper over the backed Program Kit
local-publish operation. In this environment the Program Kit checkout is
read-only source truth; publish outputs must go only to an explicit output
path outside that checkout.
